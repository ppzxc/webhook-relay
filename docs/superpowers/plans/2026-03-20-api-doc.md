# API Documentation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** OpenAPI 3.1.0 + AsyncAPI 3.0 스펙 파일과 Redoc UI를 단일 바이너리에 embed하여 `/docs`, `/docs/openapi`, `/docs/asyncapi` 엔드포인트로 서빙하고, GitHub Actions CI에서 lint 검증한다.

**Architecture:** `internal/apidocs/` 패키지에 스펙 파일과 `//go:embed` 핸들러를 함께 배치 (Go embed `..` 경로 제약 우회). `internal/adapter/input/http/router.go`에 `/docs` 라우트 그룹 추가. GitHub Actions는 `internal/apidocs/**` 변경 시 Spectral + AsyncAPI CLI 검증 실행.

**Tech Stack:** Go 1.25 `//go:embed`, chi v5, OpenAPI 3.1.0, AsyncAPI 3.0.0, Redoc v2 standalone, Spectral CLI @6, AsyncAPI CLI @3, GitHub Actions

**Spec:** `docs/superpowers/specs/2026-03-20-api-doc-design.md`

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `internal/apidocs/embed.go` | `//go:embed` 선언 + HTTP 핸들러 3개 |
| Create | `internal/apidocs/embed_test.go` | 핸들러 단위 테스트 |
| Create | `internal/apidocs/openapi.yaml` | OpenAPI 3.1.0 전체 스펙 |
| Create | `internal/apidocs/asyncapi.yaml` | AsyncAPI 3.0.0 WebSocket 스펙 |
| Create | `internal/apidocs/redoc.standalone.js` | Redoc v2 standalone JS (다운로드) |
| Modify | `internal/adapter/input/http/router.go` | `/docs`, `/docs/openapi`, `/docs/asyncapi` 라우트 추가 |
| Modify | `internal/adapter/input/http/handler_test.go` | `/docs` 라우트 테스트 추가 |
| Create | `.spectral.yaml` | Spectral OAS 3.1 ruleset |
| Create | `.github/workflows/api-doc.yml` | CI: lint openapi + validate asyncapi |

---

## Task 1: `internal/apidocs` 패키지 — 핸들러 TDD

**Files:**
- Create: `internal/apidocs/embed_test.go`
- Create: `internal/apidocs/embed.go`
- Create: `internal/apidocs/openapi.yaml` (placeholder)
- Create: `internal/apidocs/asyncapi.yaml` (placeholder)
- Create: `internal/apidocs/redoc.standalone.js` (placeholder)

- [ ] **Step 1: placeholder 파일 및 테스트 작성**

> `//go:embed`는 embed.go 작성 전에 대상 파일이 존재해야 컴파일된다.
> 테스트 파일과 placeholder 파일을 동시에 생성한다.

```bash
mkdir -p internal/apidocs
```

placeholder 파일 3개 생성:

```yaml
# internal/apidocs/openapi.yaml
openapi: "3.1.0"
info:
  title: placeholder
  version: "0.0.0"
```

```yaml
# internal/apidocs/asyncapi.yaml
asyncapi: "3.0.0"
info:
  title: placeholder
  version: "0.0.0"
```

```
# internal/apidocs/redoc.standalone.js
/* placeholder */
```

테스트 파일 생성:

```go
// internal/apidocs/embed_test.go
package apidocs_test

import (
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"

	"webhook-relay/internal/apidocs"
)

func TestOpenAPIHandler(t *testing.T) {
	req := httptest.NewRequest(http.MethodGet, "/docs/openapi", nil)
	w := httptest.NewRecorder()
	apidocs.OpenAPIHandler(w, req)

	if w.Code != http.StatusOK {
		t.Errorf("status = %d, want 200", w.Code)
	}
	if ct := w.Header().Get("Content-Type"); ct != "application/yaml" {
		t.Errorf("Content-Type = %q, want application/yaml", ct)
	}
	if w.Body.Len() == 0 {
		t.Error("body is empty")
	}
}

func TestAsyncAPIHandler(t *testing.T) {
	req := httptest.NewRequest(http.MethodGet, "/docs/asyncapi", nil)
	w := httptest.NewRecorder()
	apidocs.AsyncAPIHandler(w, req)

	if w.Code != http.StatusOK {
		t.Errorf("status = %d, want 200", w.Code)
	}
	if ct := w.Header().Get("Content-Type"); ct != "application/yaml" {
		t.Errorf("Content-Type = %q, want application/yaml", ct)
	}
	if w.Body.Len() == 0 {
		t.Error("body is empty")
	}
}

func TestRedocHTMLHandler(t *testing.T) {
	req := httptest.NewRequest(http.MethodGet, "/docs", nil)
	w := httptest.NewRecorder()
	apidocs.RedocHTMLHandler(w, req)

	if w.Code != http.StatusOK {
		t.Errorf("status = %d, want 200", w.Code)
	}
	if ct := w.Header().Get("Content-Type"); !strings.HasPrefix(ct, "text/html") {
		t.Errorf("Content-Type = %q, want text/html", ct)
	}
	body := w.Body.String()
	if !strings.Contains(body, "<redoc") {
		t.Error("body does not contain <redoc element")
	}
	if !strings.Contains(body, `spec-url="/docs/openapi"`) {
		t.Error("body does not contain spec-url=/docs/openapi")
	}
}
```

- [ ] **Step 2: 테스트 실행 — 컴파일 오류 확인**

```bash
go test ./internal/apidocs/...
```

Expected: compile error `undefined: apidocs.OpenAPIHandler` (embed.go 없음)

- [ ] **Step 3: placeholder 파일 존재 확인 (Step 1에서 생성 완료)**

```bash
ls internal/apidocs/
```

Expected: `asyncapi.yaml  embed_test.go  openapi.yaml  redoc.standalone.js`

- [ ] **Step 4: embed.go 구현**

```go
// internal/apidocs/embed.go
package apidocs

import (
	_ "embed"
	"net/http"
)

//go:embed openapi.yaml
var OpenAPISpec []byte

//go:embed asyncapi.yaml
var AsyncAPISpec []byte

//go:embed redoc.standalone.js
var RedocJS []byte

func OpenAPIHandler(w http.ResponseWriter, r *http.Request) {
	w.Header().Set("Content-Type", "application/yaml")
	w.WriteHeader(http.StatusOK)
	w.Write(OpenAPISpec) //nolint:errcheck
}

func AsyncAPIHandler(w http.ResponseWriter, r *http.Request) {
	w.Header().Set("Content-Type", "application/yaml")
	w.WriteHeader(http.StatusOK)
	w.Write(AsyncAPISpec) //nolint:errcheck
}

func RedocHTMLHandler(w http.ResponseWriter, r *http.Request) {
	w.Header().Set("Content-Type", "text/html; charset=utf-8")
	w.WriteHeader(http.StatusOK)
	w.Write([]byte(`<!DOCTYPE html><html><head><meta charset="utf-8"><title>webhook-relay API</title></head><body><redoc spec-url="/docs/openapi"></redoc><script>`)) //nolint:errcheck
	w.Write(RedocJS)                                                                                                                                                   //nolint:errcheck
	w.Write([]byte(`</script></body></html>`))                                                                                                                         //nolint:errcheck
}
```

- [ ] **Step 5: 테스트 실행 — 통과 확인**

```bash
go test -race ./internal/apidocs/...
```

Expected: `ok  webhook-relay/internal/apidocs`

- [ ] **Step 6: 커밋**

```bash
git add internal/apidocs/
git commit -m "feat(apidocs): add embed package with OpenAPI/AsyncAPI/Redoc handlers"
```

---

## Task 2: Redoc standalone JS 다운로드

**Files:**
- Modify: `internal/apidocs/redoc.standalone.js` (placeholder → 실제 파일)

- [ ] **Step 1: Redoc v2 standalone 다운로드**

```bash
curl -fL "https://cdn.jsdelivr.net/npm/redoc@2.1.5/bundles/redoc.standalone.js" \
  -o internal/apidocs/redoc.standalone.js
```

> `-f` 플래그: HTTP 오류 시 실패 (CDN 404를 HTML로 저장하는 실수 방지)

- [ ] **Step 2: 파일 크기 확인 (약 1.8MB 예상)**

```bash
ls -lh internal/apidocs/redoc.standalone.js
```

Expected: 약 1.6MB~2.0MB (Redoc v2.1.5 minified bundle)

- [ ] **Step 3: 테스트 재실행 — 여전히 통과 확인**

```bash
go test -race ./internal/apidocs/...
```

Expected: `ok  webhook-relay/internal/apidocs`

- [ ] **Step 4: 커밋**

```bash
git add internal/apidocs/redoc.standalone.js
git commit -m "chore(apidocs): add Redoc v2.1.5 standalone JS"
```

---

## Task 3: openapi.yaml 작성

**Files:**
- Modify: `internal/apidocs/embed_test.go` (OpenAPI 내용 검증 assertion 추가)
- Modify: `internal/apidocs/openapi.yaml` (placeholder → 전체 스펙)

- [ ] **Step 1: TestOpenAPIHandler에 내용 검증 assertion 추가 (실패하는 테스트)**

`internal/apidocs/embed_test.go`의 `TestOpenAPIHandler` 함수 끝에 추가:

```go
// placeholder는 version "0.0.0" — 실제 스펙은 "2026-03-20" 이어야 한다
if !strings.Contains(w.Body.String(), "webhook-relay API") {
    t.Error("openapi.yaml does not contain expected title")
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
go test -race -run TestOpenAPIHandler ./internal/apidocs/...
```

Expected: `FAIL` — placeholder에는 `webhook-relay API` 없음

- [ ] **Step 3: openapi.yaml 작성**

```yaml
# internal/apidocs/openapi.yaml
openapi: "3.1.0"

info:
  title: webhook-relay API
  version: "2026-03-20"
  description: |
    Webhook relay hub. Receives alerts from monitoring apps (Beszel, Dozzle, etc.)
    and forwards them to external channels via templates.

servers:
  - url: http://localhost:8080
    description: Local development

security:
  - bearerAuth: []

tags:
  - name: alerts
    description: Alert management
  - name: sources
    description: Source management
  - name: channels
    description: Channel management
  - name: health
    description: Health check

paths:
  /healthz:
    get:
      summary: Health check
      operationId: healthz
      tags: [health]
      security: []
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                required: [status]

  /sources/{sourceId}/alerts:
    post:
      summary: Receive alert
      operationId: postAlert
      tags: [alerts]
      parameters:
        - $ref: "#/components/parameters/sourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Source-specific JSON payload
              additionalProperties: true
      responses:
        "201":
          description: Alert created
          headers:
            Location:
              description: URL of the created alert
              schema:
                type: string
                example: /sources/beszel/alerts/01J...
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AlertCreatedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "413":
          $ref: "#/components/responses/PayloadTooLarge"
        "500":
          $ref: "#/components/responses/InternalServerError"

    get:
      summary: List alerts for a source
      operationId: listAlerts
      x-status: planned
      tags: [alerts]
      parameters:
        - $ref: "#/components/parameters/sourceId"
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: Alert list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Alert"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /sources/{sourceId}/alerts/{alertId}:
    get:
      summary: Get alert by ID
      operationId: getAlert
      tags: [alerts]
      parameters:
        - $ref: "#/components/parameters/sourceId"
        - $ref: "#/components/parameters/alertId"
      responses:
        "200":
          description: Alert found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Alert"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update alert status
      operationId: patchAlert
      x-status: planned
      tags: [alerts]
      description: |
        Allowed transition: FAILED → PENDING (manual re-queue).
        All other transitions return 422.
      parameters:
        - $ref: "#/components/parameters/sourceId"
        - $ref: "#/components/parameters/alertId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum: [PENDING]
              required: [status]
      responses:
        "200":
          description: Alert updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Alert"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /sources:
    get:
      summary: List sources
      operationId: listSources
      x-status: planned
      tags: [sources]
      responses:
        "200":
          description: Source list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Source"

  /sources/{sourceId}:
    get:
      summary: Get source by ID
      operationId: getSource
      x-status: planned
      tags: [sources]
      parameters:
        - $ref: "#/components/parameters/sourceId"
      responses:
        "200":
          description: Source found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Source"
        "404":
          $ref: "#/components/responses/NotFound"

  /channels:
    get:
      summary: List channels
      operationId: listChannels
      x-status: planned
      tags: [channels]
      responses:
        "200":
          description: Channel list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Channel"

  /channels/{channelId}:
    get:
      summary: Get channel by ID
      operationId: getChannel
      x-status: planned
      tags: [channels]
      parameters:
        - $ref: "#/components/parameters/channelId"
      responses:
        "200":
          description: Channel found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Channel"
        "404":
          $ref: "#/components/responses/NotFound"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Source-specific bearer token from config

  parameters:
    sourceId:
      name: sourceId
      in: path
      required: true
      schema:
        type: string
        example: beszel
    alertId:
      name: alertId
      in: path
      required: true
      schema:
        type: string
        example: 01J...
    channelId:
      name: channelId
      in: path
      required: true
      schema:
        type: string
        example: ops-webhook

  schemas:
    AlertStatus:
      type: string
      enum: [PENDING, DELIVERED, FAILED]
      description: |
        Alert delivery status.
        Transitions: PENDING → DELIVERED (success), PENDING → FAILED (retries exhausted),
        FAILED → PENDING (manual re-queue via PATCH).

    Alert:
      type: object
      properties:
        id:
          type: string
          description: ULID
        sourceId:
          type: string
        status:
          $ref: "#/components/schemas/AlertStatus"
        createdAt:
          type: string
          format: date-time
        retryCount:
          type: integer
          description: Number of delivery attempts
        lastAttemptAt:
          type: ["string", "null"]
          format: date-time
      required: [id, sourceId, status, createdAt, retryCount]

    AlertCreatedResponse:
      type: object
      properties:
        id:
          type: string
        sourceId:
          type: string
        status:
          $ref: "#/components/schemas/AlertStatus"
        createdAt:
          type: string
          format: date-time
      required: [id, sourceId, status, createdAt]

    Source:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum: [BESZEL, DOZZLE, GENERIC]
      required: [id, type]

    Channel:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum: [WEBHOOK, SLACK, DISCORD]
        url:
          type: string
        retryCount:
          type: integer
        retryDelayMs:
          type: integer
      required: [id, type, url, retryCount, retryDelayMs]

    Problem:
      type: object
      description: RFC 7807/9457 Problem Details
      properties:
        type:
          type: string
          example: /errors/Unauthorized
        title:
          type: string
          example: Unauthorized
        status:
          type: integer
          example: 401
        detail:
          type: string
          example: invalid or missing token for source: beszel
        traceId:
          type: string
      required: [type, title, status, detail]

  responses:
    Unauthorized:
      description: Missing or invalid bearer token
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    NotFound:
      description: Resource not found
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    PayloadTooLarge:
      description: Request body exceeds 1MB limit
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    UnprocessableEntity:
      description: Invalid status transition
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
    InternalServerError:
      description: Unexpected server error
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
go test -race ./internal/apidocs/...
```

Expected: `ok  webhook-relay/internal/apidocs`

- [ ] **Step 5: 커밋**

```bash
git add internal/apidocs/openapi.yaml internal/apidocs/embed_test.go
git commit -m "docs(apidocs): add OpenAPI 3.1.0 spec"
```

---

## Task 4: asyncapi.yaml 작성

**Files:**
- Modify: `internal/apidocs/embed_test.go` (AsyncAPI 내용 검증 assertion 추가)
- Modify: `internal/apidocs/asyncapi.yaml` (placeholder → 전체 스펙)

- [ ] **Step 1: TestAsyncAPIHandler에 내용 검증 assertion 추가 (실패하는 테스트)**

`internal/apidocs/embed_test.go`의 `TestAsyncAPIHandler` 함수 끝에 추가:

```go
if !strings.Contains(w.Body.String(), "webhook-relay WebSocket API") {
    t.Error("asyncapi.yaml does not contain expected title")
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
go test -race -run TestAsyncAPIHandler ./internal/apidocs/...
```

Expected: `FAIL` — placeholder에는 `webhook-relay WebSocket API` 없음

- [ ] **Step 3: asyncapi.yaml 작성**

```yaml
# internal/apidocs/asyncapi.yaml
asyncapi: "3.0.0"

info:
  title: webhook-relay WebSocket API
  version: "2026-03-20"
  description: |
    Inbound WebSocket API for webhook-relay.
    Monitoring apps (Beszel, Dozzle, etc.) connect and push alert payloads to the server.
    The server processes each message via ReceiveAlertUseCase.

servers:
  local:
    host: localhost:8080
    protocol: ws
    description: Local development server
    security:
      - $ref: "#/components/securitySchemes/bearerAuth"

channels:
  alerts:
    address: /sources/{sourceId}/alerts/ws
    description: |
      WebSocket channel for inbound alert delivery.
      Authenticate with Authorization: Bearer <token> on connect.
    parameters:
      sourceId:
        description: Source identifier (must match a configured source)
    messages:
      alertPayload:
        $ref: "#/components/messages/AlertPayload"

operations:
  receiveAlert:
    action: receive
    channel:
      $ref: "#/channels/alerts"
    summary: Receive alert from monitoring app
    description: |
      The server receives JSON alert payloads pushed by the connected monitoring app.
      Each message triggers ReceiveAlertUseCase.Receive(), which:
      1. Creates an Alert entity (status=PENDING)
      2. Persists to SQLite
      3. Enqueues for async delivery to configured channels
    messages:
      - $ref: "#/channels/alerts/messages/alertPayload"

components:
  messages:
    AlertPayload:
      name: AlertPayload
      title: Alert Payload
      summary: Raw JSON payload from monitoring app
      contentType: application/json
      payload:
        type: object
        description: |
          Source-specific JSON payload. Structure varies by source type.
          The relay stores and forwards this payload as-is, applying the
          channel's Go text/template for transformation on delivery.
        additionalProperties: true
        examples:
          - summary: Beszel alert example
            value:
              level: critical
              message: CPU usage exceeded 90%
              host: prod-server-01

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Source-specific bearer token. Same token used for HTTP POST endpoint.
```

- [ ] **Step 2: 테스트 재실행 — 여전히 통과 확인**

```bash
go test -race ./internal/apidocs/...
```

Expected: `ok  webhook-relay/internal/apidocs`

- [ ] **Step 5: 커밋**

```bash
git add internal/apidocs/asyncapi.yaml internal/apidocs/embed_test.go
git commit -m "docs(apidocs): add AsyncAPI 3.0.0 spec for WebSocket inbound"
```

---

## Task 5: `/docs` 라우트 — router.go 통합 TDD

**Files:**
- Modify: `internal/adapter/input/http/handler_test.go`
- Modify: `internal/adapter/input/http/router.go`

- [ ] **Step 1: 실패하는 테스트 추가**

`internal/adapter/input/http/handler_test.go` 파일 끝에 추가:

```go
func TestDocs_OpenAPI(t *testing.T) {
	router := newTestRouter(func(_ context.Context, _ domain.SourceType, _ []byte) (string, error) {
		return "", nil
	})
	req := httptest.NewRequest(http.MethodGet, "/docs/openapi", nil)
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)

	if w.Code != http.StatusOK {
		t.Errorf("status = %d, want 200", w.Code)
	}
	if ct := w.Header().Get("Content-Type"); ct != "application/yaml" {
		t.Errorf("Content-Type = %q, want application/yaml", ct)
	}
	if w.Body.Len() == 0 {
		t.Error("body is empty")
	}
}

func TestDocs_AsyncAPI(t *testing.T) {
	router := newTestRouter(func(_ context.Context, _ domain.SourceType, _ []byte) (string, error) {
		return "", nil
	})
	req := httptest.NewRequest(http.MethodGet, "/docs/asyncapi", nil)
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)

	if w.Code != http.StatusOK {
		t.Errorf("status = %d, want 200", w.Code)
	}
	if ct := w.Header().Get("Content-Type"); ct != "application/yaml" {
		t.Errorf("Content-Type = %q, want application/yaml", ct)
	}
	if w.Body.Len() == 0 {
		t.Error("body is empty")
	}
}

func TestDocs_HTML(t *testing.T) {
	router := newTestRouter(func(_ context.Context, _ domain.SourceType, _ []byte) (string, error) {
		return "", nil
	})
	req := httptest.NewRequest(http.MethodGet, "/docs", nil)
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)

	if w.Code != http.StatusOK {
		t.Errorf("status = %d, want 200", w.Code)
	}
	ct := w.Header().Get("Content-Type")
	if !strings.HasPrefix(ct, "text/html") {
		t.Errorf("Content-Type = %q, want text/html", ct)
	}
	body := w.Body.String()
	if !strings.Contains(body, "<redoc") {
		t.Error("body does not contain <redoc element")
	}
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
go test -race -run "TestDocs" ./internal/adapter/input/http/...
```

Expected: `FAIL` — 라우트가 등록되지 않아 404 응답

- [ ] **Step 3: router.go에 /docs 라우트 추가**

`internal/adapter/input/http/router.go`의 import에 추가:
```go
"webhook-relay/internal/apidocs"
```

`NewRouter` 함수 내 `r.Get("/healthz", h.Healthz)` 아래에 추가:
```go
r.Get("/docs", apidocs.RedocHTMLHandler)
r.Get("/docs/openapi", apidocs.OpenAPIHandler)
r.Get("/docs/asyncapi", apidocs.AsyncAPIHandler)
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
go test -race ./internal/adapter/input/http/...
```

Expected: `ok  webhook-relay/internal/adapter/input/http`

- [ ] **Step 5: 전체 테스트 실행**

```bash
go test -race ./... -timeout 60s
```

Expected: 모든 패키지 `ok`

- [ ] **Step 6: 커밋**

```bash
git add internal/adapter/input/http/router.go internal/adapter/input/http/handler_test.go
git commit -m "feat(http): mount /docs /docs/openapi /docs/asyncapi routes"
```

---

## Task 6: .spectral.yaml + GitHub Actions CI

**Files:**
- Create: `.spectral.yaml`
- Create: `.github/workflows/api-doc.yml`

- [ ] **Step 1: .spectral.yaml 작성**

```yaml
# .spectral.yaml
extends: ["spectral:oas"]
rules:
  oas3-valid-schema: error
  operation-description: warn
  operation-tags: warn
```

- [ ] **Step 2: GitHub Actions workflow 작성**

```yaml
# .github/workflows/api-doc.yml
name: Validate API Specs

on:
  push:
    branches: [main]
    paths:
      - "internal/apidocs/openapi.yaml"
      - "internal/apidocs/asyncapi.yaml"
      - ".spectral.yaml"
  pull_request:
    paths:
      - "internal/apidocs/openapi.yaml"
      - "internal/apidocs/asyncapi.yaml"
      - ".spectral.yaml"

jobs:
  validate-openapi:
    name: Lint OpenAPI 3.1.0
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Spectral lint
        run: >
          npx --yes @stoplight/spectral-cli@6
          lint internal/apidocs/openapi.yaml
          --ruleset .spectral.yaml
          --fail-severity error

  validate-asyncapi:
    name: Validate AsyncAPI 3.0.0
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: AsyncAPI validate
        run: >
          npx --yes @asyncapi/cli@3
          validate internal/apidocs/asyncapi.yaml
```

- [ ] **Step 3: 로컬 Spectral 검증 (CI 전 사전 확인)**

```bash
npx --yes @stoplight/spectral-cli@6 lint internal/apidocs/openapi.yaml --ruleset .spectral.yaml --fail-severity error
```

Expected: `No results with a severity of 'error' found!` (warning은 허용)

- [ ] **Step 4: 로컬 AsyncAPI 검증**

```bash
npx --yes @asyncapi/cli@3 validate internal/apidocs/asyncapi.yaml
```

Expected: 오류 없이 종료

- [ ] **Step 5: 로컬 빌드 통과 확인**

```bash
CGO_ENABLED=1 go build -o /dev/null ./cmd/server/
```

Expected: 빌드 성공

- [ ] **Step 6: 전체 테스트 최종 확인**

```bash
go test -race ./... -timeout 60s
```

Expected: 모든 패키지 `ok`

- [ ] **Step 7: 커밋**

```bash
git add .spectral.yaml .github/workflows/api-doc.yml
git commit -m "ci(api-doc): add Spectral lint + AsyncAPI validate GitHub Actions"
```
